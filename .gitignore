# API keys — never commit
.tabpfn_key

# Python environment
.venv/
__pycache__/
*.pyc

# Editor / tooling
.claude/
.DS_Store

# Large data files (use Git LFS instead)
data/processed_tabpfn.csv
data/processed_daily_metrics.csv

# Unzipped raw dataset (~1.3 GB) — comes from unzipping the LFS zip; never commit it
data/turingAI_forecasting_challenge_dataset.csv

# R history
.Rhistory

# Results (regenerated by pipeline)
results/

# Methodology drafts — only METHODOLOGY_3.md is kept in the repo
METHODOLOGY.md
METHODOLOGY_2.md
